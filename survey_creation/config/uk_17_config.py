"""
Config file specific to uk to create automated survey
"""

class specificConfig:

    header_to_add = [('sid', ['421498'], 1),
                        ('admin_email', 'olivier.philippe@soton.ac.uk', 5),
                        ('additional_languages', 'nl')]
    description_to_add = []


    def _add_data_to_list(self, original_list, list_to_add, key_to_add):
        """
        """
        for data in self.

    @property
    def main_headers(self):

       return outlist

   @property
   def main_description(self):

    main_headers
    # This value needs to be added the first element is the key, the second the value
    # The third is the position in the list index to append at the right place according
    # to the template
    main_header =
